{% extends 'base.html.twig' %}

{% block title %}Candidature{% endblock %}

{% block body %}
    <h1>Candidature</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ candidature.id }}</td>
            </tr>
            <tr>
                <th>Date_envoi</th>
                <td>{{ candidature.dateEnvoi ? candidature.dateEnvoi|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Entreprise</th>
                <td>{{ candidature.entreprise }}</td>
            </tr>
            <tr>
                <th>Intitule</th>
                <td>{{ candidature.intitule }}</td>
            </tr>
            <tr>
                <th>Date_reponse</th>
                <td>{{ candidature.dateReponse ? candidature.dateReponse|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Date_contact</th>
                <td>{{ candidature.dateContact ? candidature.dateContact|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Date_entretien</th>
                <td>{{ candidature.dateEntretien ? candidature.dateEntretien|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Resultat</th>
                <td>{{ candidature.resultat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_candidature_index') }}">back to list</a>

    <a href="{{ path('app_candidature_edit', {'id': candidature.id}) }}">edit</a>

    {{ include('candidature/_delete_form.html.twig') }}
{% endblock %}
