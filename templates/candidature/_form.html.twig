<div class="formulaire">
{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="bouton">
    <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
    </div>
{{ form_end(form) }}
</div>



